#include "config.h"

/*
config.cpp
===============================================================================
ROLE
  Placeholder translation unit for compile-time configuration.

RATIONALE
  This branch keeps configuration constants in config.h and runtime values inside
  RuntimeConfig. The file remains present to preserve the requested module layout
  and to provide a future location for persistence helpers.
===============================================================================
*/